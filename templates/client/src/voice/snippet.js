// eslint-disable-next-line no-unused-vars
import { VoiceService } from '@sinch/sdk-core';

/** @param {VoiceService} voiceService */
export const execute = async (voiceService) => {
  console.log('Snippet execution: Voice service');
};
