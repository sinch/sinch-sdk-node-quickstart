// eslint-disable-next-line no-unused-vars
import { SmsService } from '@sinch/sdk-core';

/** @param {SmsService} smsService */
export const execute = async (smsService) => {
  console.log('Snippet execution: SMS service');
};
